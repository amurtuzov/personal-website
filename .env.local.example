# Local development (MinIO + local Postgres)
NODE_ENV=development
PORT=4000

# Database (Docker Compose postgres service)
DATABASE_URL="postgresql://postgres:test_password_123@postgres:5432/photosdb?schema=public"
POSTGRES_PASSWORD=test_password_123

# Redis (Docker Compose redis service)
REDIS_URL=redis://redis:6379
# Required: password for Redis auth (used by backend/workers and Redis container)
REDIS_PASSWORD=your_redis_password

# JWT
JWT_SECRET=change-me-local-secret-please

# S3 via MinIO (Docker Compose override)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
S3_ENDPOINT=http://minio:9000
# Public endpoint used for presigned upload URLs (browser needs a host-reachable address)
S3_PRESIGN_ENDPOINT=http://localhost:9000
S3_REGION=us-east-1
S3_BUCKET=amurtuzov-photos
S3_ACCESS_KEY_ID=minioadmin
S3_SECRET_ACCESS_KEY=minioadmin
# Optional CDN/public URL (leave empty to use signed URLs)
# S3_PUBLIC_URL=localhost:9000

# Database backups (keep 2 dumps: latest + previous)
DB_BACKUP_LOCAL_DIR=./backups/db
DB_BACKUP_POSTGRES_DB=photosdb
DB_BACKUP_POSTGRES_USER=postgres
DB_BACKUP_SPACES_ENABLED=false
DB_BACKUP_SPACES_BUCKET=amurtuzov-photos
DB_BACKUP_SPACES_REGION=us-east-1
DB_BACKUP_SPACES_ENDPOINT=http://minio:9000
DB_BACKUP_SPACES_PREFIX=db-backups/postgres

# Frontend (CMS) API endpoint baked at build time
VITE_API_URL=http://localhost:4000
# Frontend (Nuxt apps) API endpoint
NUXT_PUBLIC_API_URL=http://localhost:4000
# Frontend (Photos Nuxt app) canonical base URL
NUXT_PUBLIC_BASE_URL=http://localhost:8082

# Backend CORS and form protection
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080,http://localhost:8081,http://localhost:8082,http://localhost:5173,https://amurtuzov.com,https://photos.amurtuzov.com,https://cms.amurtuzov.com
FORM_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080,http://localhost:8081,http://localhost:8082,https://amurtuzov.com,https://photos.amurtuzov.com
FORM_MIN_SUBMISSION_MS=2000
FORM_MAX_SUBMISSION_MS=43200000

# CMS basic auth for Caddy (optional for local reverse proxy)
CMS_BASIC_USER=your_cms_user
# Local-only convenience value (plain password). Keep this file off git.
CMS_BASIC_PASS_PLAIN=your_secure_cms_password
# Must be a bcrypt hash generated via:
# BCRYPT_COST=12 ./scripts/set-cms-password.sh .env.local
# ./scripts/set-cms-password.sh .env.local
# Script stores Docker Compose-safe escaped value ($$2b$$...).
CMS_BASIC_PASS=replace_with_caddy_bcrypt_hash_compose_escaped

# Contact form email (Google SMTP / Gmail app password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=your_gmail_or_workspace_account@gmail.com
SMTP_PASS=your_app_password
CONTACT_TO=you@yourdomain.com
# For Gmail, this should usually be the same as SMTP_USER
CONTACT_FROM=your_gmail_or_workspace_account@gmail.com
CONTACT_SUBJECT_PREFIX=[Contact Form]

# Telegram Ops Bot (optional)
TELEGRAM_BOT_TOKEN=your_telegram_bot_token
TELEGRAM_ALLOWED_USER_IDS=123456789
# Optional: restrict to specific chat IDs (comma-separated)
# TELEGRAM_ALLOWED_CHAT_IDS=123456789
# Optional: override service allowlist for /logs /restart /recreate
# OPS_BOT_ALLOWED_SERVICES=backend,cleanup-worker,email-worker,image-worker,redis,postgres,minio,migrate,main,photos,cms,caddy,ops-bot
